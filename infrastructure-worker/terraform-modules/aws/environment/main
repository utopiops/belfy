// DO NOT CHECK THIS IN THE REPO AS main.tf (notice the extension)

// This is just for test.
provider "aws" {
  region = "us-east-1"
}

// DO NOT CHECK THIS IN THE REPO AS main.tf (notice the extension)

// This is just for test.
provider "aws" {
  region = "us-east-2"
}

terraform {
  required_providers {
    # random = {
    #   source  = "hashicorp/random"
    #   version = "3.1.0"
    # }
    aws = {
      source  = "hashicorp/aws"
      version = "3.63.0"
    }
    
  }
  backend "s3" {
    encrypt = true
  }
}

