// TODO: DELETE
const express = require('express');
const router = express.Router();

const providerController = require('./providerController');

// router.get('/displayName/:name', providerController.getDetails);
// router.get('/displayName/:name/test', providerController.testProvider);
// router.patch('/displayName/:name/credentials', providerController.updateCredentials);
// router.post('/displayName/:name/deploy', providerController.deployProvider);

module.exports = router;
