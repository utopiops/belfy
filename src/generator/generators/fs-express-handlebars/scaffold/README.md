


project-root/
│
├── config/
│   ├── config.js        // Database configuration
│   └── express.js       // Express configuration
│
├── controllers/
│   └──  // Separate controllers for each entity
│
├── models/
│   └──  // Sequelize models for each entity
│
├── routes/
│   └──  // Route definitions for each entity
│
├── views/
│   ├── layouts/
│   │   └── main.handlebars  // Main layout file
│   └──  // Separate handlebars files for each entity
│
├── public/
│   ├── css/
│   ├── js/
│   └──  // Other static files (images, fonts, etc.)
│
└── app.js               // Main application entry point