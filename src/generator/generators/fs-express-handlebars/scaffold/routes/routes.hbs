const express = require('express');
const router = express.Router();
{{#each entities}}
const {{this.name.toLowerCase()}}Controller = require('../controllers');

// Routes for {{this.name}}
router.get('/{{this.name.toLowerCase()}}', {{this.name.toLowerCase()}}Controller.getAll);
router.get('/{{this.name.toLowerCase()}}/:id', {{this.name.toLowerCase()}}Controller.getById);
router.post('/{{this.name.toLowerCase()}}', {{this.name.toLowerCase()}}Controller.create);
router.put('/{{this.name.toLowerCase()}}/:id', {{this.name.toLowerCase()}}Controller.update);
router.delete('/{{this.name.toLowerCase()}}/:id', {{this.name.toLowerCase()}}Controller.delete);

{{/each}}
module.exports = router;
